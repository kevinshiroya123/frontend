{
  "dependencies": {
    "react-scripts": "^5.0.1"
  },
  "homepage": "https://kevinshiroya123.github.io/testing",
  "scripts": {
    "build-main-frontend": "cd main-frontend && npm install && npm run build",
    "deploy": "npm run build-main-frontend && cp -r main-frontend/build/* ./ && git add . && git commit -m 'Deploy main-frontend to GitHub Pages' && git push origin main"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}