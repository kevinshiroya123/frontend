{
  
    "name": "testing",
    "version": "1.0.0",
    "homepage": "https://kevinshiroya123.github.io/testing",
    "scripts": {
      "build-main": "cd main-frontend && npm install && npm run build",
      "deploy": "npm run build-main && git add . && git commit -m 'Deploy main-frontend' && git push origin main"
    },
    "devDependencies": {
      "gh-pages": "^6.2.0"
    }
  
  
}